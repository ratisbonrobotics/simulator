name: Publish

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: sudo apt-get update && sudo apt-get install lld -y
    - uses: actions/checkout@v3
    - run: make
    - uses: actions/upload-pages-artifact@v2.0.0
      with:
        path: ./
  deploy:
    permissions:
      pages: write
      id-token: write
    needs: build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/deploy-pages@v2
